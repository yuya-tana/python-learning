取り出す件数に上限を設けるためには
    div = soup.find_all('div'limit=2)
とすることで、取り出した文字列のなかから先頭2件だけを取得するような処理とできる。　P53


# メソッドチェーン
# news = soup.find(class_='block-news').find('h2')
続けて記述できる

pythonの対話モードでどこまでがうまく実行できているのか確かめよう 71P
    powershell上でpythonと入力して対話モードを呼び出し、コードを書いてあげると処理の流れが見える
quite()で終了

検証時には相手サイトの迷惑とならないように、サイトの内容を取り込んでそのなかで実行してみよう P73 2_8_1参考